# scalar Date
enum Status {
  ACTIVE
  COMPLETED
}

type ToDo {
  id: Int!
  title: String!
  status: Status!
  createdAt: String!
}

type Query {
  todos: [ToDo]
}

type Mutation {
  add(title: String!): ToDo
  delete(id: Int!): Boolean
}
